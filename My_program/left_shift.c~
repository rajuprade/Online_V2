#include<stdio.h>

int main()
{
  int x = 3; // 0 0 1 1 
  int y ,n;

  y = x << 2; // left shift by two digit 
  
  n = x >> 2; // right shift by two digit 

  fprintf(stderr," Value of Y is ### %d\n",y);

  fprintf(stderr," Value of N is ### %d\n",n);
  
return 0;

}
